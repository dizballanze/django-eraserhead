============
Installation
============

At the command line::

    $ easy_install django-eraserhead

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-eraserhead
    $ pip install django-eraserhead
